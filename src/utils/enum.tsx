export enum eDesignerMode {
    CONFIGURE = 1,
    CONFIGURE_PLAY = 2,
    RUNNING = 3,
}

export enum eOperationType {
    DIRECT = 1,
    BOOLEAN = 2,
    PERCENTAGE = 3,
    DEPENDENT = 4,
    CUSTOM = 5,
}